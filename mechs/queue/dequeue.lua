--[[
  Dequeue prototype. Old code. Unused - no real need.
]]

local interface =
  {
    add_first = add_first,
    add_last = add_last,
    extract_first = extract_first,
    extract_last = extract_last,
    is_empty = is_empty,
  }
