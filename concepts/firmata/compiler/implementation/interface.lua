return
  {
    emit = request('emit'),
    emit_bytes = request('emit_bytes'),
    emit_string = request('emit_string'),
    i2c_emit_request = request('i2c_emit_request'),
    output = '', -- output file/device
  }
